import { FragmentActiveDirective } from './fragment-active.directive';

describe('FragmentActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FragmentActiveDirective();
    expect(directive).toBeTruthy();
  });
});
